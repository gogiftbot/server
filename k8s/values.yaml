container:
  repository: ghcr.io/gogiftbot

hostname: gogift.space

back:
  image: server
  pullPolicy: "Always"
  replicas: 1
  type: ClusterIP
  port: 4000
  tag: release-2.0.2

# front:
#   image: web
#   pullPolicy: "Always"
#   replicas: 1
#   type: ClusterIP
#   port: 3000
#   tag: release-1.1.9-1